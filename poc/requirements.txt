# BlueWallet ISAAC CREATE PoC — runtime dependencies.
ecdsa==0.19.2        # pure-python secp256k1 (reference / fallback path)
mnemonic==0.21       # BIP39 wordlist + mnemonic <-> entropy/seed
coincurve==21.0.0    # optional native libsecp256k1 fast path (auto-detected)
pytest==9.1.1        # test runner (dev only)
